# Portfolio Maker environment variables
# Copy to .env and adjust before running.

# Set to "false" to perform a real Vercel deployment.
# When "true" (default), the deploy command prints a dry-run instead.
PM_DEMO_MODE=true
